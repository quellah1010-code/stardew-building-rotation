# 空牛棚旋转实验版 0.1.0

本包首次接入真实建筑状态、移动输入、碰撞、门和朝向保存，使用占位图。**已编译并核对补丁目标签名，尚未完成游戏内旋转验收。请在测试存档里验证普通空牛棚。** 之前 0.0.2 的加载、拿起／取消及移动后坐标读取已有用户实机截图；那次测试的是豪华牛棚，不代表本包支持豪华牛棚旋转。

## 安装与操作

1. 完全退出游戏，移除旧的 `Mods/BuildingRotation.Diagnostics`，再将新 ZIP 内的 `BuildingRotation.Prototype` 文件夹放进 `Mods`。两包的 UniqueID 相同，不要同时安装。保留 Let's Move It。
2. 通过 SMAPI 启动，日志应显示 `Building Rotation — Prototype 0.1.0` 与 `ordinary empty Barn prototype attached; placeholder graphics`。若显示 `prototype unavailable` 或红色错误，先反馈该段日志。
3. 进入单人测试存档，使用 **普通 Barn**：未升级、未施工、没有动物居住，室内没有物品、家具或角色。Big Barn／Deluxe Barn 不进入旋转模式；它们仍可按原搬动 mod 移动。当前实际接入版本为游戏 1.6.15、SMAPI 4.5.2、Let's Move It 0.6.20。
4. 按平时的 Let's Move It 操作拿起建筑，**松开拿起时的按键**。需单选、关闭复制／多选，搬动键设为单独的鼠标左键；不新增启动快捷键。
5. 再按住左键约 **0.35 秒**，向左或右拖动至少约 24 屏幕像素，旋转一档。一次长按最多转一档；松手后仍处于预览。随后用一次短点击放下。鼠标抓住的那格会继续贴着鼠标，不按矩形左上角跳动。
6. 原搬动 mod 的取消键（默认 Esc）取消预览；失焦、换地点、失去选择或保存前也会丢弃未提交预览。已经成功放下的建筑位置和朝向会保留。

绿色框表示当前检查允许放置，红框表示被地形、物件、角色或门前空间挡住；最终点击时还会重新检查。本版要求地面清理好，不自动删草、作物、物件或挖掘点。人物自己也不能站在候选占地或门外检查区域里。

放下后的金棕色格子是建筑的**占位外观**，不是正式四视图素材。标签标明门朝向；青色格标记实际人门，外侧青框是门前区域。走到青框处、面向青色门格，按正常交互方式进门。房间内部保持原样，出门回到当前朝向的门外；门外被堵时应拒绝出门。

## 这轮看什么

先测试一栋，再测试另一栋是否受影响。检查四向转动、鼠标抓取位置、非法位置拒绝放下和取消恢复；退出编辑后绕着建筑走，确认碰撞对应格子和当前占地，而不是旧位置。四个方向各进出一次同一个房间。最后在测试存档中过夜保存、退出并重进，检查位置、占地、门与朝向是否一致。

先反馈“拿起后有没有彩色预览、能否长按转向、能否放下”，以及出现的问题和日志。`br_status` 仍可在 SMAPI 控制台中输入，报告版本与当前读取状态。若出现带 `rollback` 的错误，退出本次测试且不要保存，将错误段反馈；回滚已经编写，但尚未在真实游戏中注入故障验证。

这版不支持动物、升级、联机、其他建筑或跨地点搬移，也没有 Reveal 和正式素材。测试期间保持该牛棚为空，不给它安排动物或升级。其他搬动功能继续由 Let's Move It 处理；已旋转的牛棚在不满足本包编辑条件时会阻止原搬动提交，避免留下不一致的出口。

## 接入和验证记录

- `GameRotationHost` 从真实 `Building.GetData()` 读取碰撞和门区，复用 Data／Core 的不可变映射。实例按对象身份登记，位置、尺寸、门、元数据、房间身份及出口变化使快照修订号更新。
- 预览不改游戏建筑。放置时在候选格上调用原版 `isBuildable`，用仅当前调用有效的补丁上下文排除本建筑旧占地；检查地图范围、物件、地形、角色、额外区域和门前净空。不调用会清除世界物件的原版建造流程，也不改变共享建筑数据。
- 提交只写当前建筑的位置、宽高、人门／动物门坐标、自己的 `modData` 朝向键和室内返回 warp。出现异常时尝试恢复所有已记录字段，回滚异常会明确报告。动物门坐标同步不代表动物行为已适配。
- Harmony 在 Let's Move It 的按下即放置方法执行前取得输入控制，原渲染预览在本会话中停止，使用现有 RotationEditor／RotationSession 手势和提交逻辑。取消通过原选择清除流程同步。
- 对已标记的普通 Barn 接入逐格通行、完整矩形碰撞、人门接近方向和占位绘制；加载建筑数据、重建房间或更新出口后重新应用布局。使用游戏原有 `Building.modData` 存储朝向，游戏原有字段保存位置。
- 室外返回在原传送执行前检查落点，完成场景切换后按人物真实碰撞框再次核对并校准像素位置和朝向；若淡入期间出口被占用，尝试返回原房间的标准入口。正常进门保留原来的坐骑／锁定／声音等逻辑。
- 标准 Release 构建通过。`BuildingRotation.GameAudit` 直接读取实际 DLL 元数据，**13/13 补丁目标签名符合**；它不执行游戏，也不证明 Harmony 运行安装成功。游戏写入、回滚、传送和存读档的验收仍待本轮用户实测。此前 100 项独立自检不是本轮游戏测试。

仍需重点留意输入事件顺序、渲染批次、进出门落点及其他 mod 的影响；检测到兼容问题时按日志修复，不以“签名存在”代替行为验证。当前普通空牛棚的同地点移动只做必要字段更新，不调用可能清除地图内容的放置回调；第三方依赖这些回调的行为尚未验证。

开发复现：

```sh
dotnet build src/BuildingRotation.Smapi/BuildingRotation.Smapi.csproj -c Release -p:GamePath="/path/to/game"
dotnet run --project tools/BuildingRotation.GameAudit -- "/path/to/game" "/path/to/LetsMoveIt.dll"
python scripts/package-diagnostics.py
```

包中仅包含四个自有 DLL、manifest 和本说明；游戏、SMAPI、Harmony、Let's Move It 的 DLL、用户截图和反编译源码均不发布。
