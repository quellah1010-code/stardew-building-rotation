# 继续开发时从这里接

本记录更新至 2026-09-10，包含此前真实数据、四视图待审状态及本轮室内／马厩讨论。开始续做先读取仓库最新提交和工作区修改；不要假定临时目录、SDK 或进程会跨执行保留。

## 最新完成：本机文件核对与诊断入口首次构建

已读取新上传的游戏 ZIP、Let's Move It ZIP 和启动截图。游戏 DLL 为 1.6.15.24356、.NET 6，搬动 mod 为 0.6.20。**截图运行 SMAPI 4.5.2，上传的 StardewModdingAPI.dll 则为 4.3.2，连 Constants API 字符串也为 4.3.2。** 原因未确认；不要直接判定用户当前安装损坏，也不能宣称已用 4.5.2 编译。

已有入口用上传 DLL 完成标准 MSBuild Debug／Release 构建，补了 GameLaunched 及公开 IsLoaded 状态报告；没有游戏加载、没有旋转补丁、没有发布可玩包。Core／Runtime 未改，不重复运行此前 100 项自检。已核对上传 Let's Move It 的选择、按下即放置、鼠标抓取偏移及建筑移动成功后清空目标的控制流。

**接下来需要用户从截图正在运行的安装目录补取** `StardewModdingAPI.dll`、`MonoGame.Framework.dll`、`xTile.dll`、`StardewValley.GameData.dll`、`smapi-internal` 文件夹，及已安装 Let's Move It 的 `config.json`（如有）。具体版本、哈希、构建方式和真实方法位置都集中在 [runtime-adapter.md](runtime-adapter.md)。上传 DLL、截图、日志及反编译原文没有提交公开仓库。本节优先于下方历史“未编译／没有程序集”的记录。

## 前一批：棚屋操作方案与空牛棚运行流程

本轮按用户选择继续 xhigh 的工作节奏，精简重复工具调用，但保留充分的设计边界讨论。此前马厩左右侧有马／无马及 Shed 布局对照已保存，本轮补了[棚屋四步操作图和中文文案](art/use-studies/README.md)：开关只影响预览，打包核对在最终放置时确认，空房跳过打包；均为候选方案，不是游戏 UI。

新增 `BuildingRotation.Runtime`，用现有手势、编辑与布局核心串起实例快照、预览／提交、朝向字典重建、日常碰撞／门／返回查询。新增 12 组，**100/100 独立检查通过**；五个无游戏依赖工程的标准 MSBuild 构建通过。新增 `BuildingRotation.Smapi` 的项目、manifest 和诊断入口源码；因缺少游戏／SMAPI DLL，未编译或运行入口。真实宿主 `IRotationHost`、输入提供方、Harmony、绘制、传送和游戏存档绑定仍未实现，不能把内存宿主测试叫实机通过。详见 [runtime-adapter.md](runtime-adapter.md)。

首个可玩目标仍是普通空牛棚。接下来用匹配的游戏／SMAPI 程序集和搬动 mod 版本编译诊断入口，再实现真实宿主；棚屋室内开关、打包或保留摆放整体转向仍待用户取舍，不扩大首版范围。现有 Barn v5、Stable v4、Shed v3 四视图保持不变；马厩正背面带马使用图和精确骑出通路仍未完成。

本次未重跑原 ZIP 全包检查：当前临时路径的文件无法作为 ZIP 打开，使用已提交并经前轮核验的真实字段摘要；历史 89/89 不能写成本批结果。下方美术／数据内容为此前历史，若与本节进度不一致，以本节和链接的当前代码为准。

## 前一批美术：单横栏与 2/3 侧宽

当前待审为 Barn v5、Stable v4、Shed v3。用户明确认可截图中较宽马厩的侧宽，并要求每侧只留一根横木条、其他保持；该截图对应上一轮第三次宽度尝试，不是窄版 v3。原图已作为 `stable-four-views-user-selected-wide.png` 留档，v4 以它编辑：保留一根横栏、斜撑和底梁，右侧宽度不变、左侧差 1 像素。不要再以之前 52% 的版本替代用户选图。

Shed 按用户要求试到正面 2/3：v3 右／左侧约为正面 68.7%／67.0%。正面外轮廓 473 像素，右侧 325、左侧 317；两侧有 8 像素差，屋面和墙体高度有重绘。两张均完整解码、目视检查并记录提示词和来源，不代表逐像素局部编辑、完整投影或游戏验收。累计 15 次生成，保留 10 张板（当前 3 张，历史／来源 7 张）。

用户也提出马厩水草交互、侧面看不到马和内部摆放的疑问，随后明确要求“先把现有的 progress 跑完”。因此本轮只完成上述美术与记录，不实施无限水草、马匹挪位、侧面透明、室内旋转或摆设重排。已核对的原版马机制见 [Wiki](https://stardewvalleywiki.com/Animals#Horse)：胡萝卜属于额外加速；给水／喂食加好感可见于 [Horse Overhaul 作者说明](https://www.nexusmods.com/stardewvalley/mods/7911)，尚未确认用户安装该 mod。马的可见性、真实停放点与骑乘通路需另议；既有“室内不转”基线仍保留，用户的疑问不记成已决定更改。

## 项目方向与此前记录

用户指出侧视图仍是老问题，要求暂缓透明度，先给几个建筑画正／左／右／后四视图，由助手完成一批并同步 GitHub，再一起看。此指示取代此前“先修北门 Reveal 再拆透明层”的美术顺序，不取消最终 Reveal 设计。首个实机原型仍是单人普通空牛棚，不因美术同时画三栋而扩大兼容承诺。

本批已完成 Barn、Shed、Stable 共三套四视图待审板（12 个方向），见 [turnarounds/README.md](art/turnarounds/README.md)。用户在本轮开始逐张审阅，先来 Barn：在右视图附属木构顶端补交叉木条、互换错误的左右标签、去掉左视图屋顶上方露出的远侧条。v4 完成首次三处修订后，用户继续指出“你那两个木板的透视有问题”。最新 v5 以 v4 为来源，把原先立在红墙前的大叉压入附属小棚屋面的浅窄投影范围，让上方红墙重新露出；保留已修正的标签和左侧屋顶轮廓。已查看生成结果，仍待用户审阅，不能记为用户通过；细部纹理有重绘，不是逐像素局部编辑。该阶段为牛棚 v5、棚屋 v1、马厩 v2，包含反馈共 8 次生成／修订；随后棚屋和马厩的侧宽修订见下文。提示词、图片检查及结构参考一并保存。

最新用户反馈是马厩和棚屋侧面太窄，并明确要求修改前接上 GitHub、读完相关记录，完成后同步。本轮已读 README、接续、设计、美术、四视图、Content 核对／映射、路线图以及相关尺寸／提示词／检查记录。以实际占地 Stable 4×2、Shed 7×3 为校准起点，新增两份宽度轮廓 SVG；旧提示词反复强调窄侧面，却未稳定控制实际生成尺寸。

上一轮为 Barn v5、Stable v3、Shed v2，共 3 张当前板。Stable 两侧外轮廓均约为正面的 52.1%（原 41.2%～43.2%）；Shed 约 44.5%～44.9%（原 36.3%）。本轮 5 次生成，淘汰 3 次 Stable 偏差稿，最终保留两张；四视图批次累计 13 次生成、7 张留档。三份当前图的左右标签均已统一。图片完整解码、轮廓宽度测量和目视检查完成，没有新的游戏内或 C# 测试。

宽度有所改善，但生成并非逐像素局部编辑：正背面也有重绘，Shed 正面外轮廓从 421 增至 474 像素。Stable 低侧屋面长度未完全匹配轮廓参考。不得声称完整投影、构件尺度、正式锚点或原生贴图已验收；当前版本均待用户继续审图。具体结果和提示词在四视图目录。

左右名称按建筑本身的左／右侧使用；此前将入口朝向当成视图名称，标签反了。当前三张均上右为 RIGHT、下左为 LEFT，位置没换。历史 Shed v1／Stable v2 仍有旧标签错误，不是已审通过。游戏 Facing 枚举和门的计算不受文字纠正影响。剩余屋面体积感、跨视图比例和马厩开口表现仍待审，不要把“成批待审”写成正式素材验收通过。

新增 `scripts/build-turnaround-reference.py`，仅用标准库输出三个推定体块的四向 SVG。体块高度和隐藏结构是参考估计，不是原版模型；该脚本不修改游戏数据。三个 SVG 的重生成一致性、XML／画布边界与关键方向检查通过；三张 PNG 完整解码并检查四个面。没有本轮游戏内测试，也未重跑此前 89 项 C# 自检。

## 已完成的代码与数据

原有碰撞布局／多格门核心和 63 项自检保留。提交 `093bbd3f43b061e7864035dcaa9eda0f5d07fb65` 新增 `src/BuildingRotation.Data`、`tools/BuildingRotation.ContentAudit`：从上传 ZIP 或事实摘要读取真实字段，映射到不可变核心布局，并生成素材地面锚点。先读 [content-mapping.md](content-mapping.md) 和 [art/README.md](art/README.md)，不要重复实现。

验证为 **89/89 通过**：原有 63 项、16 项数据检查、9 项素材坐标检查、1 项可选原始 ZIP 全包核对。没有 ZIP 时运行 88 项。直接 Roslyn 编译和标准 MSBuild 构建均已通过；标准构建零警告、零错误，`dotnet run --no-build --no-restore` 执行成功。没有游戏内测试。

本包 25 个定义可读，24 个能映射当前几何布局，不代表实机支持 24 种建筑。Farmhouse 的 (9,4) 框外阻挡完整保留，但核心转换明确拒绝；不能裁掉它。`OnlyNeedsToBePassable` 已进入带类型的附加放置区，四向变换不丢标志；`AdditionalTilePropertyRadius` 保留但还没驱动游戏查询。Barn／Shed／Stable 真实字段及门位测试通过，基础／升级牛棚的动物门 X 差异也已验证。

已生成三种建筑共 12 方向的 SVG 地面／门区指导和锚点 JSON，保存在 `docs/art/templates`，三张预览均已检查。Barn 的 112×112 主体与 112×128 图集分开处理，Shed 的全零源区解析为整张 112×128。画布上部余量属于草稿约定，不是已确认的侧视投影；没有新产出原生分层像素画。包内仍没有游戏／SMAPI DLL，精确版本未核验。

## 前一批 Barn 概念探索（历史）

收到“继续”后用内置图像生成工具完成 5 次 Barn 绘制／修订，保留朝东 v2、朝北普通 v1、朝北 Reveal v2 共 3 张概念稿。见 [concepts/README.md](art/concepts/README.md) 及同目录完整提示词、PNG 检查 JSON。原始游戏参考图未提交。

本轮 5 张 PNG 全部完整解码，但均为 RGB，无 alpha 或透明色；朝东的两次真实透明请求失败，棋盘格是实色。输出不是所要求的原生画布，精确门槛未验收，所有图都是扁平合成。北向 Reveal v2 已淡化上墙并保留墙脚，但门口木板仍在内侧、地面表现仍有歧义。**这是可继续修改的概念稿，不是完成四向素材、透明分层或游戏 Reveal。** 本轮源码和正式模板未改，也未重跑此前 89 项自检。

## 接着做什么

既有[使用示意与棚屋流程](art/use-studies/README.md)已备齐本批审阅内容；优先按[运行层接入记录](runtime-adapter.md)推进诊断入口编译、真实宿主与空牛棚实机功能。既有四视图按反馈修订，不能把新示意当作功能已完成，也不等待全部美术或室内功能才开始接入。

1. 按用户最新节奏逐张审图，当前待审图为 `barn-four-views-v5.png`、`stable-four-views-v4.png`、`shed-four-views-v3.png`，均在 `docs/art/turnarounds`；接着按用户反馈修订。继续按反馈修形体、屋面和比例；马厩局部显露按新方案做有明确用途的示意，不恢复此前反复微调牛棚 Reveal 的工作顺序。交叉木条必须随附属屋面投影，不能把正面大叉直接贴到侧面红墙上。四向形体确定后再做原生画稿、透明分层和入口视觉重构。正式地面坐标仍以 `docs/art/templates` 为依据，改门位必须同步布局与底稿，不能只改画面。用户不需要代画。
2. 先按最新文件核对结果对齐 SMAPI 4.5.2 与附属引用，重编译并做诊断入口游戏加载检查，再实现真实宿主与 Let's Move It 0.6.20 适配。移动状态与鼠标输入协调尚未实现，不要因独立手势通过就标记兼容。
3. 需要扩展到 Farmhouse 时，先实现并验证框外阻挡表示与查询；当前拒绝是有意边界。素材快照仅为首帧子集，完整动画、条件、自定义图层纹理及皮肤要从实际数据补齐后再用于渲染。农场规划器仅作交互参考，不另造网站、照搬快捷键或上传用户存档。
4. 同步 [roadmap.md](roadmap.md) 与实际提交，说明完成内容、验证方式和剩余依赖。若别的执行已更新仓库，保留并协调其改动，不能强推或覆盖。

首个可玩目标仍是 PC 键鼠、单人、普通空牛棚，可先用占位图；动物出入、NPC 路径、升级、联机和全建筑素材都仍在后续范围。用户不需要替助手写代码或画图。

## 验证与接续

在具备 .NET SDK 的环境中，按 README 运行核心自检。现有可复现记录为：不提供原始素材包时 88 项，提供由用户授权的原始素材包时 89 项；这不代表已完成游戏内测试。

本批新增了 Runtime 与诊断入口源码，标准构建及 100 项独立自检已通过，SMAPI 工程除缺依赖检查外未编译。后续代码验证按仓库文档在当前环境重新构建；不要依赖先前会话的临时文件或运行进程。原始游戏素材包仍不得上传公开仓库。

## 继续保持的设计边界

不新增全局启动快捷键，旋转编辑跟随已适配的建筑移动模式；日常外观、碰撞、门和 Reveal 在退出编辑后仍应工作。首版室内不转，后续可选转向仍待定；普通建筑背面 Reveal 与马厩侧面显露探索分别处理。背面显示的门必须对应真实门；透明分层不改变通行。

通用几何不代替游戏世界检查。`TryGetExit` 成功不是已经传送，也不证明从门到外部路线畅通；游戏实际取整后的碰撞框还需验证。当前类型级数据保持不可变，游戏对象身份、更新、缓存、存档和实际补丁仍由后续接入完成。

公开仓库保存自有源代码和文档；游戏 DLL、解包原版素材、个人存档与含私人信息的日志不提交。自动续做的调度保存在本会话的任务中；本文件是接续记录，本身不会启动任务。
